{
  "name": "youdaonote-export",
  "version": "0.1.0",
  "description": "Batch export Youdao Cloud Notes to Markdown",
  "scripts": {
    "build": "webpack --mode production",
    "build:dev": "webpack --mode development",
    "build:watch": "webpack --mode development --watch",
    "pack": "cd dist && zip -r ../build/youdaonote-export.zip *"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.2",
    "webpack": "^5.97.0",
    "webpack-cli": "^6.0.0"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
